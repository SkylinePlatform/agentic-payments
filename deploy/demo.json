{
  "$comment": [
    "The demo topology: what `make demo` starts, in what order, and how it knows each one is up.",
    "",
    "Order matters. The collector comes first because every role emits into it, and a role that",
    "starts before it is listening loses whatever it emits in the meantime — there is no retry,",
    "on purpose, because an event log that pushed back on the roles would be the coupling ADR",
    "0003 forbids. The frontend comes last because it proxies to the collector and there is",
    "nothing to look at until the roles are running.",
    "",
    "The two agents come after the roles and before the frontend. Every role above them has either",
    "answered its health check or been reported as a stub by then, so both run against a stack that",
    "is up; what they produce is in the collector's history before the frontend starts, which is",
    "what the 512-entry ring and the SSE replay are for — a browser attaching afterwards still sees",
    "the whole transaction.",
    "",
    "Two processes, and one would have done: cmd/agent takes -buy and -watch together and sequences",
    "them, Human Present first. What decides it is what happens when the first one fails. A failed",
    "purchase returns from that binary's run(), so a single process would exit before the watch had",
    "authorised anything — a regression in the flow that already works would take the flow this",
    "milestone is about off the screen with it, and the demonstration would show neither. Split,",
    "each one fails on its own, and the Human Present path still fails on every `make demo`, in",
    "front of whoever was about to take a screenshot, rather than only in CI.",
    "",
    "agent-watch is started first, and the reason is the merchant's clock rather than anything about",
    "the two flows. agent-buy has no health check — it is not a server, and the entry below says why",
    "— so the runner has nothing to wait on and gives it a flat two seconds to fall over before",
    "moving on. Those two seconds used to sit between the merchant starting and the watch taking its",
    "baseline price, which is fine against a thirty second step and is not fine against a three",
    "second one: see the paragraph on -step's second number below. Measured on this manifest, the",
    "order below leaves 0.45 to 0.46 seconds of merchant clock before the baseline where the old one",
    "left 2.45 to 2.49 against a 3s floor — the two seconds reclaimed are that grace exactly.",
    "",
    "What it costs is that the log now opens with Human Not Present rather than with one complete",
    "Human Present transaction, which is the preference this order used to serve. That preference is",
    "worth less than the margin: the beat the whole screen exists for is the verifier's refusal at",
    "$210, and a run where it silently does not happen is worse than a log whose first transaction is",
    "the watched one. It was never a precondition either way — the two carry different correlation",
    "IDs, so they are readable in any order and were already allowed to overlap, since two seconds is",
    "in practice longer than a purchase against a stack that is up but was never a guarantee.",
    "",
    "agent-buy still runs against a stack that is entirely up, and still at the opening $240: it now",
    "starts after agent-watch has answered its health check, which that binary does only once it has",
    "authorised and taken its baseline, so the sequencing is if anything stricter than before.",
    "",
    "Each is named for the flag it runs, so an entry here and the binary it starts are one word",
    "apart.",
    "",
    "agent-watch states -poll where the merchant above it states -step and -step-max, because",
    "those numbers only mean anything together. The watch triggers on the merchant's step",
    "changing, so a poll longer than a step would step over the $210 the verifier is meant to",
    "refuse and go straight from $240 to $189 — beat 5 of the built scenario, the one where a",
    "verifier says no, would simply not happen. Issue #158 made -step and -step-max a range rather",
    "than a fixed duration: each price now holds a random width drawn once from [-step, -step-max]",
    "instead of the same thirty seconds every time — see merchant.NewJitteredSchedule — so what",
    "-poll has to stay under is not the pair's average but its floor, -step itself, the shortest a",
    "price can ever hold. One second against a three-to-six-second range is at least three polls a",
    "step even in the worst case, and the whole watched-refused-bought flow now completes in",
    "roughly twelve seconds — two transitions at up to six seconds each — rather than the two and a",
    "half minutes two fixed thirty-second steps used to take. Shortening -step here still has to",
    "bring -poll down with it, on the same terms as before: -poll must stay comfortably below",
    "-step, which a reader can only notice if every number in the pair is in this file.",
    "",
    "**-step has a second number to clear, and it is what decided the start order above.** The",
    "merchant's schedule starts when the merchant starts — merchant.NewDemoService reads it off the",
    "clock at construction — but agent-watch is further down this list, and the price it takes as its",
    "baseline is whatever is in force by the time it gets there. internal/agent's watch attempts only",
    "on a step it has not already seen, so a baseline that is already the $210 loses beat 5 just as",
    "completely as a slow poll would, and just as silently: one attempt, bought at $189, no error",
    "anywhere. Measured across eight runs, 0.45 to 0.46 seconds of merchant clock now pass between",
    "the merchant starting and that baseline — process spawn, two health checks and the agent's own",
    "authorisation round trip, and nothing that waits on a timer. Against a 3s floor that is a factor",
    "of about six. Before agent-watch was moved above agent-buy it was 2.45 to 2.49 seconds, half a",
    "second of margin, because the runner's two second grace for a process it cannot health-check sat",
    "inside that window; the difference between the two measurements is that grace to within a few",
    "milliseconds, which is what confirms it was the whole of the problem.",
    "",
    "It is still the number to look at first if this demonstration ever shows one attempt instead of",
    "two, and the three things that would eat it are a much slower machine, a process inserted above",
    "agent-watch, and a model actually answering. agent-watch's own `-interpreter auto`, added below,",
    "puts a round trip inside this same window whenever GEMINI_API_KEY happens to be exported —",
    "exactly what `-interpreter gemini` has always cost, just reachable now without anybody asking",
    "for it by name. internal/demo's TestTheMerchantsFirstPriceOutlastsTheStackComingUp is what fails",
    "on the second of those: it derives the floor from the runner's own constants and now also",
    "requires that floor to be zero, which is exactly the property the order above buys and the only",
    "part of this paragraph a test can prove — it reasons about the topology's shape, not about what",
    "a running agent-watch does once it is up, so neither a slow machine nor a model round trip is",
    "anything it can see. TestThePollStaysUnderTheShortestPriceHold pins the paragraph above. Neither",
    "can measure the rest, which is why it is written down here.",
    "",
    "**This margin argument assumes the one-shot schedule `merchant.NewJitteredSchedule` builds",
    "today** — one random width drawn per step, spent once the steps run out. A schedule that cycles",
    "instead changes what \"before the baseline\" means to measure against, and revisiting the numbers",
    "above for that shape belongs to whichever change makes the schedule cycle, not to this one.",
    "",
    "The four identifiers the agent can also be given are not restated, for the opposite reason.",
    "They have to agree with cmd/merchant, cmd/credprovider and cmd/mpp, none of which is given one",
    "here either — so both sides are the flag's default today, and a value written here could only",
    "ever drift from the binary it names.",
    "",
    "The merchant's `-catalogue` is not restated either, and on that same rule rather than by",
    "oversight. It defaults to `../deploy/catalogue.json`, which resolves from the `backend` working",
    "directory below, so a value here would say what the binary already says. What makes `-step`,",
    "`-step-max` and `-poll` the exception is that they only mean anything together: the group has",
    "to be read as one, which a reader can only do if every number in it is in this file. A path has",
    "no partner.",
    "",
    "agent-watch alone gets `-addr`, which is what turns the agent into a server: it serves the",
    "console API of #137, so a browser can start a watch and poll where each mandate in it stands.",
    "The flag defaults to empty meaning do not serve, so agent-buy below is byte-identical to what it",
    "was — a Human Present purchase approved at the moment of purchase has no standing authorisation",
    "for a console to track, and that process has no business listening. Having no -addr is also why",
    "it has no health check, and so why the start order above puts it last of the two. The watch this",
    "entry starts",
    "runs inside the console rather than beside it, so the terminal and the browser are looking at",
    "one run; frontend/vite.config.ts proxies /watches here, which is why the port is stated in both",
    "files and has to move in both.",
    "",
    "agent-watch alone also gets `-interpreter auto`, and on the same reasoning as `-addr`: only this",
    "process's interpreter choice reaches anybody. agent-buy's Human Present flow never calls one at",
    "all — buyOnce takes no interpreter argument, and content() builds the payment straight from the",
    "quoted price — so the flag would sit on that entry doing nothing, which is worse than not writing",
    "it: a reader would credit agent-buy with a decision it cannot make.",
    "",
    "`gemini` is not the choice here, on its own refusal: `-interpreter gemini` with no key refuses to",
    "start at all, which would break `make demo` on every machine that has none, and most do. `auto`",
    "never refuses for a missing key — it reads GEMINI_API_KEY if one is exported and degrades to the",
    "scripted table if not, naming which one it picked in the startup banner either way.",
    "",
    "**This is the manifest's choice, not the binary's default.** `cmd/agent`'s own default stays",
    "`scripted`, argued at length in that package's doc, because a *default* that read the operator's",
    "shell would make every caller of the binary non-reproducible for a reason nothing in the",
    "repository states. A manifest is a different thing: this file is reviewed and committed, so a",
    "reader sees `-interpreter auto` sitting in agent-watch's args the same as every other flag here.",
    "That is the deliberate naming interpreterFor's doc asks for in place of a silent fallback, not a",
    "second way of getting one.",
    "",
    "**What it costs is the golden numbers, on any machine with a key exported.** With one present,",
    "agent-watch reads free text and answers with whatever the model returns — no longer",
    "`interpret.Scenarios()`'s fixed five, and not guaranteed to match a screenshot already in this",
    "repository. That is the point of adding `auto` here rather than leaving it unreachable from any",
    "manifest, and it is safe for the same reason `-interpreter gemini`'s refusal to fall back exists",
    "in the first place: the console now says which mode it is in before the box is touched —",
    "GET /examples answers empty under a model and non-empty under the scripted table, and",
    "frontend/src/routes/Console.tsx renders one sentence or the other from it — so a screenshot",
    "stays attributable to the mode that produced it even when that mode is not the scripted five.",
    "It says neither sentence until that call has actually answered, which is the half that makes it",
    "evidence rather than a guess: a browser that could not reach this agent sees exactly the",
    "emptiness a model produces, and a screen that read the two the same way would promise free text",
    "on behalf of a process that is down.",
    "",
    "Without a key, `auto` builds `interpret.Demo()` — the same value `-interpreter scripted` returns",
    "— so a machine with none still shows exactly the golden five, and every number a screenshot of",
    "this demonstration is read for is untouched. One line of terminal output does change for it, and",
    "deliberately: the startup banner reads `auto — scripted — …` where it used to read `scripted —",
    "…`, because naming the flag as well as the arm it resolved to is the whole of what makes a run",
    "under `auto` attributable to `auto`.",
    "",
    "The merchant runs with `-demo-controls`, so `POST /demo/advance` moves its clock on by",
    "exactly `-step` — the floor of the range, not a random draw of its own; see",
    "merchant.DemoOptions.StepMax. It moves everything that merchant was built to read: the",
    "prices, offer expiry, mandate expiry, challenge freshness, and the window its idempotency",
    "store forgets records after. That is deliberate — the control says time passed rather than",
    "that the price changed — and it is a property of how merchant.NewDemoService wires the",
    "process, not of the endpoint. Off by default everywhere else: an endpoint that moves a",
    "verifier's clock belongs in a demonstration and nowhere near anything real.",
    "",
    "Nothing in `make demo` presses this control automatically — the automated flow lets the wall",
    "clock carry the schedule at its own -step/-step-max cadence, which is what makes the twelve",
    "second target true without anybody clicking anything. The control is for a presenter skipping",
    "ahead by hand, and skipping ahead under a jittered schedule is imprecise in one specific way:",
    "since a step's own random width can land anywhere up to `-step-max`, one press of exactly",
    "`-step` is not guaranteed to cross it, and an occasional second press may be needed. That is",
    "accepted rather than solved — solving it would mean the control asking the schedule for its",
    "own next boundary instead of moving by a fixed duration, which is a bigger change than a",
    "presenter's convenience needs.",
    "",
    "It needs an `Idempotency-Key`, like every unsafe method here, and a repeat under the same key",
    "replays the first answer instead of advancing again — which is what makes a double-clicked",
    "button harmless and what makes two steps need two keys.",
    "",
    "`implemented: false` marks a binary that is still a stub. The runner starts it anyway and",
    "expects it to exit — which is how a stale flag becomes visible rather than silently wrong.",
    "Flip it in the pull request that implements the role."
  ],
  "processes": [
    {
      "name": "collector",
      "kind": "infrastructure",
      "summary": "gathers protocol events and streams them to the frontend over SSE",
      "note": "Demo infrastructure. Not an AP2 role and not a TAP identity party — see ADR 0003.",
      "implemented": true,
      "dir": "backend",
      "command": "bin/collector",
      "args": [
        "-addr",
        "127.0.0.1:8085"
      ],
      "health": "http://127.0.0.1:8085/healthz"
    },
    {
      "name": "surface",
      "kind": "role",
      "summary": "Trusted Surface — shows the interpretation and takes the user's signature",
      "issue": "9",
      "implemented": true,
      "dir": "backend",
      "command": "bin/surface",
      "args": [
        "-addr",
        ":8084",
        "-collector",
        "http://127.0.0.1:8085/events"
      ],
      "health": "http://localhost:8084/.well-known/jwks.json"
    },
    {
      "name": "merchant",
      "kind": "role",
      "summary": "Merchant — prices the route and verifies the Checkout Mandate",
      "note": "Advance the schedule instead of waiting it out — a fresh key per step, since a repeat replays: curl -X POST -H 'Idempotency-Key: step-1' http://localhost:8081/demo/advance",
      "issue": "9",
      "implemented": true,
      "dir": "backend",
      "command": "bin/merchant",
      "args": [
        "-addr",
        ":8081",
        "-surface",
        "http://localhost:8084",
        "-collector",
        "http://127.0.0.1:8085/events",
        "-step",
        "3s",
        "-step-max",
        "6s",
        "-demo-controls"
      ],
      "health": "http://localhost:8081/.well-known/jwks.json"
    },
    {
      "name": "credprovider",
      "kind": "role",
      "summary": "Credential Provider — issues a payment credential scoped to one checkout",
      "issue": "9",
      "implemented": true,
      "dir": "backend",
      "command": "bin/credprovider",
      "args": [
        "-addr",
        ":8082",
        "-surface",
        "http://localhost:8084",
        "-collector",
        "http://127.0.0.1:8085/events"
      ],
      "health": "http://localhost:8082/.well-known/jwks.json"
    },
    {
      "name": "mpp",
      "kind": "role",
      "summary": "Merchant Payment Processor — verifies the Payment Mandate",
      "issue": "9",
      "implemented": true,
      "dir": "backend",
      "command": "bin/mpp",
      "args": [
        "-addr",
        ":8083",
        "-surface",
        "http://localhost:8084",
        "-collector",
        "http://127.0.0.1:8085/events"
      ],
      "health": "http://localhost:8083/.well-known/jwks.json"
    },
    {
      "name": "registry",
      "kind": "role",
      "summary": "local agent registry — resolves an agent's key reference",
      "issue": "26",
      "implemented": false,
      "dir": "backend",
      "command": "bin/registry"
    },
    {
      "name": "proxy",
      "kind": "role",
      "summary": "verifying proxy at the merchant edge — checks RFC 9421 signatures",
      "issue": "30",
      "implemented": false,
      "dir": "backend",
      "command": "bin/proxy"
    },
    {
      "name": "agent-watch",
      "kind": "role",
      "summary": "Shopping Agent — authorises once, then watches the price and buys on its own",
      "note": "Human Not Present, beats 4 to 6: $240.00 watched, $210.00 refused with a signed receipt, $189.00 bought. Advance the merchant to see it sooner. It also serves the console API: GET http://127.0.0.1:8086/watches lists every watch, and a new one is curl -X POST -H 'Idempotency-Key: one-click' -d '{\"prompt\":\"buy a flight to Palma when it drops below $200, this summer\"}' http://127.0.0.1:8086/watches. GET http://127.0.0.1:8086/watches/{id}/attempts/1/presented is the four chains that attempt presented, each beside the verifier it was addressed to — it is what #21's Mandate Inspector reads, and the chains are deliberately not on the polled view",
      "issue": "15",
      "implemented": true,
      "dir": "backend",
      "command": "bin/agent",
      "args": [
        "-surface",
        "http://localhost:8084",
        "-merchant",
        "http://localhost:8081",
        "-credprovider",
        "http://localhost:8082",
        "-mpp",
        "http://localhost:8083",
        "-collector",
        "http://127.0.0.1:8085/events",
        "-addr",
        "127.0.0.1:8086",
        "-watch",
        "-interpreter",
        "auto",
        "-poll",
        "1s"
      ],
      "health": "http://127.0.0.1:8086/healthz"
    },
    {
      "name": "agent-buy",
      "kind": "role",
      "summary": "Shopping Agent — runs one Human Present purchase, then waits",
      "issue": "9",
      "implemented": true,
      "dir": "backend",
      "command": "bin/agent",
      "args": [
        "-surface",
        "http://localhost:8084",
        "-merchant",
        "http://localhost:8081",
        "-credprovider",
        "http://localhost:8082",
        "-mpp",
        "http://localhost:8083",
        "-collector",
        "http://127.0.0.1:8085/events",
        "-buy"
      ]
    },
    {
      "name": "frontend",
      "kind": "ui",
      "summary": "three lanes, the Mandate Inspector and the Trusted Surface consent screen",
      "issue": "20",
      "implemented": true,
      "dir": "frontend",
      "command": "npm",
      "args": [
        "run",
        "dev"
      ],
      "health": "http://127.0.0.1:5173/",
      "url": "http://127.0.0.1:5173"
    }
  ]
}
