@AGENTS.md

The import above pulls [AGENTS.md](AGENTS.md) into context automatically —
project context, hard rules and the protocol corrections your training data
probably gets wrong. That file is the single source of truth for agent
instructions in this repository; nothing project-specific belongs here.

Read it before writing any code.
